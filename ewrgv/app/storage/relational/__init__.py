"""
app/storage/relational/__init__.py
------------------------------------
Relational database models and session management.

Uses SQLAlchemy (to be configured).  ORM models live here; they are
intentionally NOT the same as domain models (Paper, Evidence, etc.).
Mapping between ORM and domain objects happens in repositories/.
"""
