# configs/

Configuration files (YAML, TOML, JSON) for different deployment environments.

Examples:
- `default.yaml`  — default pipeline parameters
- `dev.yaml`      — development overrides
- `test.yaml`     — test environment settings
