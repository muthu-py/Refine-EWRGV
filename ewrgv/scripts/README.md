# scripts/

Utility scripts for development, data management, and deployment.

Examples:
- `ingest_papers.py`  — manually trigger literature ingestion
- `build_index.py`    — build or rebuild the vector index
- `run_pipeline.py`   — run the full pipeline from CLI
